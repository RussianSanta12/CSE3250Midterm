# CSE3250Midterm
python3 -m venv .venv
source .venv/bin/activate
pip install --upgrade pip
pip install pyjokes
pip install flask

When starting this assignment, I followed the inclass instructions to configure and setup an Azure Ubuntu VM I realized that if I wanted to do all the work on that vm I would quickly burn through my money. I started by creating the HTML files that I could open locally to ensure they looked the way I wanted them to. I made the CSS inline for simplicity. This was my first time styling using a coding environment like VS Code. I learned that it has a color selector, which made the process a lot easier. After some research and time designing the html files i then moved to my flask backend. I've recently been learning more about Flask, so I thought it would be a good fit for the simplicity of this site. I started off by making the simple routes just to view both pages and allow the buttons within the html files to send between the two pages, then I worked on getting pyjokes to work in the backend, followed by adding the joke to the history. to display the history of jokes on the history page, I had to learn how to write a simple for loop within the html file that contained the information of the history list. During each testing phase, I would push my code to GitHub to then clone it on my vm to test the functionality of the site. Once O completed the final version of my site, I began to work on the report and realized that I was supposed to make a react site and began converting it to the starter code you provided. I had some complications getting PyJokes to work, and that is when you told me that a Flask site would suffice for this midterm. 

